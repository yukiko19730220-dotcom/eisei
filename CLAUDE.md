# CLAUDE.md

このリポジトリで作業する AI アシスタント（Claude Code 等）向けのガイドです。

## 1. このリポジトリは何か

**まりも本店 衛生管理（HACCP）アプリ** — 飲食店の「HACCP の考え方を取り入れた衛生管理」の
日々の記録・衛生管理計画の掲示・保健所提示用の月次帳票出力を、スマートフォン 1 台で完結させる
ための Web アプリです。

- 対象ユーザー：店舗スタッフ（非エンジニア、日本語のみ）。主にスマートフォンから利用。
- UI・コメント・データはすべて**日本語**。`<html lang="ja">`。
- 記録データは**サーバーに送らず、その端末の localStorage にのみ保存**されます。

### リポジトリ構成

```
.
└── index.html   # アプリ全体（HTML + CSS + JS、約 700 行）
```

**これで全部です。** ビルド設定・依存パッケージ・テスト・CI・アセットは一切ありません。
`index.html` 単体で完結する静的ページです（GitHub Pages 等でそのまま配信、
またはファイルをブラウザで直接開くだけで動作）。

## 2. 開発ワークフロー

### 動作確認

ビルド不要・依存なし。次のいずれかで確認します。

```bash
# ブラウザで直接開く
open index.html            # macOS
xdg-open index.html        # Linux

# もしくは簡易サーバー（ファイル入出力の挙動を実環境に近づけたい場合）
python3 -m http.server 8000   # → http://localhost:8000/
```

確認のポイント（自動テストが無いため、変更時は手動で確認してください）:

1. **モバイル幅**（DevTools のデバイス表示、375px 前後）でレイアウトが崩れないこと。
   下部タブと保存バーが固定表示のため、`body { padding-bottom }` との干渉に注意。
2. **4 つのタブすべて**（記録 / 計画 / 履歴 / 設定）を開いて JS エラーが出ないこと。
3. **リロード後もデータが残る**こと（localStorage への自動保存）。
4. 帳票を触った場合は**印刷プレビュー**（Ctrl/Cmd+P）で A4 横のレイアウトを確認。
5. 設定タブの「バックアップを書き出す / 読み込む」が往復すること。

### ブランチ・コミット

- 既定ブランチは `main`。作業は指示されたフィーチャーブランチ（例：`claude/...`）で行い、
  `git push -u origin <branch>` でプッシュします。
- コミットメッセージは既存の履歴に合わせて**日本語**で、変更内容が分かる一文にします。
  例：`衛生管理アプリ（HACCP記録）を公開`
- Pull Request はユーザーから明示的に依頼された場合のみ作成します。

## 3. アーキテクチャ

`index.html` は 3 ブロック構成です。

| 位置 | 内容 |
|---|---|
| `<style>` | CSS 変数（`:root` の `--green` 等）とコンポーネント CSS、末尾に `@media print` |
| `<body>` | ヘッダー → 4 つの `<section class="view">` → 保存バー → 下部タブ → `#printArea` |
| `<script>` | `"use strict"` の単一スクリプト。バナーコメント `/* ===== 名前 ===== */` でセクション分け |

### 画面（タブ）の仕組み

`<section class="view" id="view-<名前>">` を `show(name)` が `.active` の付け替えで切り替えます。
名前は `record` / `plan` / `history` / `settings` の 4 つで、下部 `nav.tabs` の
`data-v` 属性と一致させる必要があります。`show()` はタブごとに
`renderPlan()` / `renderHistory()` / `renderSettings()` を都度呼び直します（記録タブは初期化時に構築）。

### イベントの流儀

- 動的生成した DOM は**インライン `onclick` から関数を呼ぶ**方式です
  （例：`onclick="setVal('g_hand','ok')"`）。
  そのため**すべてのハンドラ関数はスクリプト直下のトップレベル関数宣言のままにしてください。**
  モジュール化・IIFE への封じ込め・`const fn = () => {}` への置換は、インライン属性から
  参照できなくなるため行わないこと。
- 静的な入力欄（`recBy` / `recNote` / `recDate` / `setShop` 等）だけは
  スクリプト末尾側で `addEventListener` を直接張っています。スクリプトは `</body>` 直前に
  あるため、**この方式が使えるのは HTML に静的に存在する要素のみ**です。

### レンダリングは innerHTML の文字列組み立て

`itemHTML()` / `renderTempRows()` / `renderPlan()` / `printMonth()` などは
テンプレートリテラルで HTML 文字列を作り `innerHTML` に代入します。フレームワークは使いません。
既存のこのスタイルを踏襲してください。

## 4. データモデル（最重要）

### localStorage キー

```js
const LS_SET="marimo_eisei_settings_v1";   // 設定
const LS_REC="marimo_eisei_records_v1";    // 日々の記録
```

末尾の `_v1` はスキーマ版です。**既存データと互換性の無い構造変更をする場合は、
キーを `_v2` に上げたうえで旧キーからの移行処理を書いてください。**
黙って構造を変えると、利用者の端末に溜まった記録が読めなくなります。

### 記録（`records`）の形

```js
records = {
  "2026-08-19": {
    checks: { g_uke: "ok", c_heat: "ng", m_recv: "na" },  // "ok" | "ng" | "na"
    memos:  { c_heat: "追加加熱で対応" },                  // 任意（遅延生成）
    temps:  { 0: "4.2", 3: "-18" },                        // 冷蔵庫ごとの実測値（文字列）
    tmarks: { 0: "ok", 3: "ok" },                          // "ok" | "ng" | ""
    by:   "半田 友紀子",                                    // 記録者
    note: "特記事項"
  }
}
```

- 日付キーは `YYYY-MM-DD`（`todayStr()` がローカル時刻で生成。`toISOString()` は
  UTC ずれで前日になるため使わないこと）。
- `checks` / `memos` のキーは後述のチェック項目 `id`。
- **`temps` / `tmarks` のキーは `settings.fridges` の配列インデックス**です。

> ⚠️ **既知の落とし穴**：温度記録が冷蔵庫の「名前」ではなく「インデックス」に紐づいているため、
> 設定タブで冷蔵庫を削除（`delFridge()` は `splice`）すると以降のインデックスがずれ、
> **過去の記録の温度が別の庫のものとして表示されます。**
> 温度まわりを触る際はこの前提を壊さないか確認し、修正するなら安定 ID への移行＋
> 既存レコードのマイグレーションをセットで行ってください。

### 設定（`settings`）の形

`defaultSettings` に店舗の実データ（屋号・営業者・所在地・営業許可・冷蔵庫 9 台・従業員）が
入っています。これは**初回起動時の初期値**であり、既に使っている端末では localStorage の値が
優先されます。つまり `defaultSettings` を書き換えても既存利用者の表示は変わりません。

```js
fridges: [{ name:"台下冷蔵庫", type:"冷蔵" }, { name:"マグロストッカー", type:"冷凍", limit:-50 }]
```

### 保存タイミング

入力のたびに `saveRecords()` / `saveSettings()` が即座に localStorage へ書き込みます
（保存ボタンは無し）。`saveRecords()` は `flash()` で「✓ 保存しました hh:mm」を数秒表示します。
新しい入力 UI を足すときも**この即時保存の流儀に合わせてください**。

## 5. チェック項目の定義と追加方法

チェック項目は 5 つの配列で宣言されており、`allDefs` に集約されます。

| 配列 | 対象 | 描画先 |
|---|---|---|
| `generalDefs` | 一般衛生管理（全業種共通） | `#generalItems` |
| `ccpDefs` | 重要管理（区分 1〜3、`grp` を持つ） | `#ccpItems` |
| `meatDefs` | 食肉販売業 | `#meatItems` |
| `noodleDefs` | 麺類製造業 | `#noodleItems` |
| `sozaiDefs` | そうざい製造業 | `#sozaiItems` |

定義のフィールド：

```js
{
  id: "g_health",        // 一意。records に永続化されるキー
  q:  "見出し（設問）",
  hint: "補足説明",
  grp: "区分1",          // ccpDefs のみ。ピル表示
  alwaysMemo: true,      // 「否」でなくてもメモ欄を常時開く
  quick: ["定型文1","定型文2"]   // ワンタップでメモに追記するチップ
}
```

**追加するとき**：該当配列に要素を足すだけで、記録画面・月次帳票の列（`printMonth()` の `cols`）
に自動で反映されます。ID の接頭辞は業種に合わせます（`g_` / `c_` / `m_` / `n_` / `s_`）。

**変更・削除するとき**：`id` は過去の記録の中に文字列として残っています。
**既存の `id` をリネーム・再利用しないでください**（過去記録の該当欄が空になります）。
文言（`q` / `hint`）の変更は安全です。

業種カード単位の「まとめて該当なし」ボタンは `markNA('meat'|'noodle'|'sozai')` で、
`markNA` 内のマップに配列が登録されている必要があります。

## 6. 温度判定のルール

```js
tempLimit(f)  // f.limit が数値ならそれ。無ければ 冷凍 = -15℃、冷蔵 = 10℃
isTempBad(i,v) // 実測値 > 基準値 なら「否」
```

- 数値を入力すると `onTemp()` が ○/× を**自動で付けます**。
- ○/× ボタンは同じボタンの再タップで解除（`setTempMark()`）。数値だけ・記号だけの入力も許容します。
- 基準は「以下」で判定（`n > limit` が NG）。冷凍庫の負値も同じ式で正しく判定されます。

## 7. 印刷・出力（保健所提示用）

- `#printArea` は通常 `display:none`、`@media print` でのみ表示されます。
  画面用の `header` / `nav` / `.view` などは印刷時に `display:none !important` になります。
- `printMonth(m)` … 指定月の全日 × 全チェック項目 × 全冷蔵庫の一覧表と、
  「否・メモ」の対応記録表を `#printArea` に組み立てて `window.print()`。用紙は `@page { size:A4 landscape }`。
- `printPlan()` … 計画タブの HTML から画面用クラスを剥がして印刷。
- チェック項目や冷蔵庫を増やすと**横に列が増えて A4 に収まらなくなります**。
  項目を追加したら必ず印刷プレビューで確認してください（`#printArea th,td` の `font-size:9.5px` が調整箇所）。

## 8. コーディング規約

- **依存を追加しない。** ライブラリ・CDN・ビルドツールは入れず、単一 HTML ファイルを維持します
  （店舗の端末でオフラインでも動くことが要件のため）。
- 変更は `index.html` 内で完結させます。ファイルを分割しないこと。
- **日本語**：UI 文言・コメントはすべて日本語。専門用語は HACCP／保健所の用語に合わせます
  （「良 / 否 / 該当なし」「一般衛生管理」「重要管理」など）。
- CSS は `:root` のカスタムプロパティ（`--green` `--ng` `--line` 等）を使い、色をハードコードしない。
- 既存の記法に合わせる：`function` 宣言、セミコロンあり、詰め気味の 1 行スタイル、
  セクション区切りのバナーコメント。
- タップ領域は指で押せる大きさ（既存ボタンの `padding:10px` 前後）を維持。
- 設定タブに表示している「アプリのバージョン: 1.0」は静的な文字列です。
  機能を追加した際は必要に応じてここも更新します。

### セキュリティ上の注意

`escapeHtml()` は印刷帳票のメモ・特記事項にのみ適用されています。屋号・冷蔵庫名・従業員名などは
`innerHTML` にそのまま埋め込まれるため、`<` や `'` を含む入力で表示が壊れます
（単一利用者・ローカル保存のため実害は限定的ですが、その周辺を触るときは
`escapeHtml()` の適用範囲を広げる方向で直してください）。

## 9. 変更時のチェックリスト

- [ ] 依存を増やしていない／`index.html` の 1 ファイル構成を保っている
- [ ] localStorage のスキーマを壊していない（壊すならキー版上げ＋移行処理）
- [ ] 既存のチェック項目 `id` をリネーム・削除していない
- [ ] インライン `onclick` から呼ばれる関数をトップレベルに保っている
- [ ] スマホ幅で 4 タブすべてを開き、コンソールにエラーが無い
- [ ] リロード後もデータが残る／バックアップの書き出しと読み込みが往復する
- [ ] 帳票を触ったら印刷プレビュー（A4 横）を確認した
